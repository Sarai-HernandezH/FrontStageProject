import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
    container:{
        flex:1,

    },
    listContainer:{
        paddingHorizontal: 20,
    },
    imageBackground: {
        flex: 1,
        width: '100%',
        height: '100%',
        resizeMode: 'cover'
    },
});